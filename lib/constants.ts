/**
 * List of accepted libraries that this library will support.
 * The plan is to support more libraries in the future.
 * For now, we are only supporting react-hook-form.
 */
export const acceptedLibraries = ["react-hook-form"] as const;
